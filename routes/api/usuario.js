const express = require('express');
/*const { Router } = require('express');
const usuarioController = require('../../controller/usuarioController');
const router = require('./entrada');

//Configurando la acción de distintos verbos
router.get('/datos_usuario', usuarioController.DetallesUsuario);
router.post('registrarse', usuarioController.NuevoUsuario);
router.post('inicio_sesión', usuarioController.Inicio_Sesión);

//Exportando la configuración de las rutas
module.exports = router;*/